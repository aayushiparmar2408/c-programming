#include<stdio.h>
int main(){
    char name[] = "my playlist";
    int songs = 20;
    float time = 4.5;
    printf(" %s has %d songs and average duration is %f minutes.",name, songs, time);
    return 0;
}